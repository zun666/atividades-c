#include <stdio.h>

int main(){
    int numeros[5] = {10,20,30,40,50};
    
    printf("posicao 0 : %d\n", numeros[0]);
    printf("posicao 1 : %d\n", numeros[1]);
    printf("posicao 2 : %d\n", numeros[2]);
    printf("posicao 3 : %d\n", numeros[3]);
    printf("posicao 4 : %d\n", numeros[4]);
    
    return 0;
}